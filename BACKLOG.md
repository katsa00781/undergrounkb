# UGKettlebell Pro — Backlog

> Ez a fájl a projekt **jelenlegi állapotát** rögzíti, és ide gyűjtjük a **jövőbeni fejlesztéseket** is.
> Frissítsd, amikor egy feladatot elkezdesz, befejezel, vagy új ötlet merül fel.
>
> Utolsó frissítés: **2026-09-19** (Gyakorlattár: FMS gyakorlatnevek magyarra fordítva)

---

## Jelölések

- `[ ]` nyitott · `[~]` folyamatban · `[x]` kész
- Prioritás: 🔴 magas · 🟡 közepes · 🟢 alacsony

---

## 1. Jelenlegi állapot (snapshot)

### Működő funkciók (route-ok alapján)
- Auth: regisztráció, login, meghívó-elfogadás (`/invite/:token`), PKCE
- Dashboard, Profil
- Edzésnapló (`/log`), Edzésnaptár (`/calendar`), Saját edzések (`/my-workouts`)
- Haladáskövetés (`/progress`), Célok (`/goals`)
- Időpontfoglalás (`/appointments`) + admin kezelés (`/appointments/manage`)
- Edzéstervező (`/workout-planner`) + 3 generátor al-oldal:
  - Sablon (`/template-generator`)
  - Periodizált (`/periodized-generator`)
  - Pwron (`/pwron-generator`)
  - Longevity (`/longevity-generator`)
- Programok (`/programs`, `/programs/:id`) — microciklus-alapú többhetes programok
- Gyakorlatkönyvtár (`/exercises`, `/exercises/:id`)
- FMS felmérés (`/assessment`) — oldalankénti pontozás + clearing tesztek
- FMS eredmények és riport (`/fms-results`, `/fms-results/:userId`) — PDF letöltés + e-mail küldés csatolmánnyal
- Felhasználókezelés (`/users`, admin)
- Polar integráció (`/polar/callback`) — legutóbbi commit
- Kettlebell komplex builder + Cardió (legutóbbi commitok)
- Edzésmegosztás (workout sharing)

### Tech állapot
- Build: **zöld** (`npm run build` sikeres); lint: **0 error** (22 örökölt warning); typecheck (`tsc --noEmit`): **zöld**
- Tesztek: **Vitest, 164 unit teszt zöld 13 fájlban** (`npm test`) — generátor, taxonómia, FMS pontozás/riport/értékelés/korrekció/PDF tiszta logika
- TODO/FIXME a kódban: 0 db (a `workouts.ts:473` gyakorlatnév-feloldás megoldva)
- **33 Supabase migráció** a `supabase/migrations/`-ban
- Edge Functions: `polar-oauth`, `polar-sync`, `send-fms-report`
- Új futásidejű függőség: `jspdf` + `jspdf-autotable` (lazy chunk, a fő bundle nem hivatkozza); `public/fonts/Manrope-{Regular,Bold}.ttf` (~48 KB/súly)
- A korábbi 100 SQL/shell fix-szkript a `scripts/legacy/`-ba archiválva; a gyökérben már csak `build.sh`, `deploy.sh` maradt
- A 29 db egyszeri „fix/summary/status" Markdown-jegyzet a `docs/legacy/`-ba archiválva; a gyökérben már csak a 13 érdemi md maradt (core + setup/deployment guide-ok)

---

## 2. Takarítás — EL KELL VÉGEZNI 🔴

> **Döntés (2026-06-24):** A `workoutGenerator.fixed.ts` a fő/aktív generátor. A `workoutGenerator.ts` NEM kell.

- [x] **`src/lib/workoutGenerator.ts` törlése.** Megerősítve: sehol nem volt rá import-hivatkozás (a kód mindenhol a `.fixed`-et használja). Törölve, CLAUDE.md pontosítva. ✅ 2026-06-24
- [x] **Gyökérkönyvtár takarítása (100 SQL/shell fix-szkript).** A `scripts/legacy/` mappába archiválva (`git mv`, így a history megmarad). A `package.json` `fix:*` / `reset:*` / `check:*` szkriptjei az új `scripts/legacy/` útvonalakra állítva; a `build.sh` és `deploy.sh` a gyökérben maradt. ✅ 2026-06-24
- [x] **Stale fájlok törlése** (verziókövetésbe nem valók): mind a 6 fájl törölve. ✅ 2026-06-24
  - `README.md.new`
  - `src/pages/ProgressTracking.tsx.new`, `ProgressTracking.tsx.backup`
  - `src/pages/Profile.tsx.bak`
  - `src/pages/WorkoutPlanner.fixed.tsx.bak`
  - `src/pages/ExerciseLibrary.tmp.tsx.bak`
- [x] **Duplikált service-ek (használaton kívüliek) törlése**: `emailService-new.ts` (0 import) és `appointmentService.ts` (0 import) törölve. ✅ 2026-06-24
- [x] **`users.ts` vs `userService.ts` összevonása**: az egyetlen külsőleg használt export (`isCurrentUserAdmin`, RPC `is_admin`-alapú) átemelve a `users.ts`-be; a `fmsService.ts` importja átállítva; a `userService.ts` (és a benne lévő, sehol nem importált Profile-segédek) törölve. Build + typecheck zöld. ✅ 2026-06-24
- [x] **`src/lib/workouts.ts:473` TODO**: a `getWorkoutProgressTrend` most batch-lekérdezéssel (`id -> name` map, egyetlen `exercises` query) oldja fel a valódi gyakorlatneveket; ismeretlen id-nál a régi fallback (id) marad. ✅ 2026-06-24
- [x] **Nagy fájlok refaktorálása** (CLAUDE.md is jelöli): ✅ 2026-06-25
  - [x] `WorkoutPlanner.tsx` (2303 → 920 sor) — komponensekre bontva ✅ 2026-06-25
  - [x] `workoutGenerator.fixed.ts` (1620 → 242 sor) — modulokra bontva ✅ 2026-06-25
  - [x] `exerciseService.ts` (869 → 225 sor) — `src/lib/exerciseTaxonomy/` modulokra bontva ✅ 2026-06-25
  - [x] `ProgressTracking.tsx` (748 → 141 sor) — `components/progress/` komponensekre + `progressTrackingHelpers.ts`-be bontva ✅ 2026-06-25
- [x] **Supabase teszt-/segéd-fájlok konszolidálása**: 3 halott fájl törölve (`supabaseConnection.ts` — `SupabaseConnectionManager`, 0 import; `utils/testConnection.ts` — 0 import; `lib/testSupabaseConnection.ts` — csak a halott `testConnection.ts` importálta). Megmaradt a 2 ténylegesen használt: `supabaseTest.ts` (UserManagement) és `supabaseUtils.ts` (megosztott `handleSupabaseError` / `testSupabaseConnection`). Build zöld. ✅ 2026-06-24

---

## 3. Minőség / stabilitás 🟡

- [x] **Build + lint zöld állapot ellenőrzése és CI-ba kötése** ✅ 2026-06-25: build + lint zöld (0 ESLint error, 22 örökölt warning). A meglévő `.github/workflows/build-test.yml` kiegészítve `npm test` lépéssel (lint → test → build sorrend).
- [x] **Tesztlefedettség: kritikus generátor-logikára unit tesztek** ✅ 2026-06-25: Vitest 2 bevezetve (`vitest.config.ts`, `npm test` / `test:watch` / `test:coverage` szkriptek). 41 unit teszt 5 fájlban a tiszta üzleti logikára: `exerciseCategorizer` (kategorizálás, random/first választás), `fmsCorrections`, `focusPresets` (periodizáció + szekció-alkalmazás), taxonómia `filters` (szűrés, FMS-fókusz) és `metadata` (címkék). Közös `fixtures.ts` factory-k.
- [x] **RLS policy-k auditja minden táblán** ✅ 2026-06-25: statikus (migrációk) + élő (Supabase advisor) audit. **Eredmény: tiszta** — mind a 21 UGKettlebell tábla RLS-enabled, és az élő biztonsági advisor 0 RLS-találatot ad rájuk (nincs hiányzó policy, nincs „always true" permisszív policy). A `polar_connections`/`cardio_sessions` szándékosan korlátozott (kliens csak DELETE/SELECT, írást a service-role Edge Function végzi — dokumentálva a migrációban). Lásd lentebb a kapcsolódó nyitott találatokat (függvény search_path, séma-drift).
- [x] **Hibakezelés egységesítése (toast magyarul)** ✅ 2026-06-25: a 17 natív blokkoló `alert()` hívás (4 cél-/edzés-komponensben: `GoalsManagement`, `GoalsDashboard`, `EnhancedGoalForm`, `PersonalWorkoutTracker`) lecserélve a domináns `react-hot-toast` `toast.success()` / `toast.error()` hívásokra. Maradék: lásd a két párhuzamos toast-rendszer tételt lentebb.
- [x] **`.env.example` naprakészsége (Polar + EmailJS változók)** ✅ 2026-06-25: ellenőrizve — minden használt `import.meta.env.VITE_*` változó szerepel benne (EmailJS×3, Polar×2, Supabase×2). Naprakész, módosítás nem kellett.

### Nyitott biztonsági / konzisztencia találatok (audit melléktermék, 2026-06-25)

- [ ] **FMS riport e-mail élesítése** 🔴: a kód kész és a PDF-generálás működik, de a **küldés addig nem él**, amíg nincs beállítva a Gmail alkalmazásjelszó és deployolva a függvény. Teendő: `supabase secrets set EMAIL_TRANSPORT=smtp GMAIL_USER=… GMAIL_APP_PASSWORD=…` + `supabase functions deploy send-fms-report` (lépésről lépésre: [docs/fms_report_email_setup.md](docs/fms_report_email_setup.md)). Az Edge Function Deno-kódja **lokálisan nem volt típusellenőrizve** (nincs telepítve Deno) — az első deploy validálja. Ha a Gmail SMTP nem járható (pl. Workspace tiltás), a transport az `EMAIL_TRANSPORT=resend` secrettel kódmódosítás nélkül cserélhető.
- [ ] **Függvény `search_path` hardening** 🟡: ~30 saját `SECURITY DEFINER` Postgres-függvény (`is_admin`, `handle_new_user`, `get_polar_status`, `is_current_user_admin`, `create_user_invite`, `use_invite_token`, `validate_invite_token`, `join_appointment`/`leave_appointment`, `increment/decrement_participants`, `update_goal_*`, `sync_exercise_derived_taxonomy_assignments`, stb.) a Supabase advisor szerint `function_search_path_mutable` (privilégium-eszkaláció kockázat). Megoldás: külön migrációban `ALTER FUNCTION ... SET search_path = public, pg_temp`. (Megjegyzés: a Supabase projekt megosztott egy kosárlabda-statisztika alkalmazással — annak tábláit/függvényeit NE bántsuk.) **Részben kezelve 2026-08-01-én**: az újonnan bevezetett `public.current_user_is_admin()` már rögzített `search_path`-tal készült — a régi `is_admin(uuid)` / `get_user_role(uuid)` páros viszont továbbra is hardening-re vár.
- [ ] **Séma-drift: hiányzó migrációk** 🔴: a `goals`, `goal_completions` és `pending_invites` táblákat a kód használja (`.from('goals')` stb.) és az élő DB-ben léteznek (RLS-enabled), de **nincs hozzájuk migration a `supabase/migrations/`-ban**. Friss DB-felálláskor hiányoznának. Megoldás: a meglévő élő séma exportálása új migration fájlokba (CREATE TABLE + RLS policies).
- [ ] **Két párhuzamos toast-rendszer** 🟢: a `react-hot-toast` (`<Toaster>` a `main.tsx`-ben, a komponensek többsége) mellett a shadcn `components/ui/use-toast` (`<Toaster>` az `App.tsx`-ben) is mountolva van, ezt már csak a `supabaseUtils.ts` használja (a `fmsService.ts` 2026-08-01-én törölve). Érdemes egy rendszerre konszolidálni (javasolt: react-hot-toast).
- [ ] **Projektszintű Supabase warningok** 🟢: „Leaked Password Protection Disabled" (kapcsold be a Supabase Auth beállításoknál) és „vulnerable Postgres version" (frissítés elérhető) — megosztott infra, döntés a tulajdonossal.
- [x] **Mozgásminta (`movement_pattern`) adat-drift** 🟡 ✅ 2026-06-27: 24 erőedzés-gyakorlat olyan `movement_pattern` értéket használt, ami nincs a UI `MOVEMENT_PATTERN_OPTIONS`-ban → nem jelentek meg kanonikus címkével a szűrőben, és a derived-trigger CASE sem fogta meg őket (csak `strength` tag maradt). Új `20260627123000_normalize_legacy_movement_patterns.sql` migráció a kanonikus leképezéssel: `anti_extension`→`stability_anti_extension`, `anti_lateral_flexion`→`stability_anti_flexion`, `loaded_carry`→`gait_stability`, `corrective`→`local_exercises`, `locomotion`→`gait_crawling`. Az `AFTER UPDATE OF movement_pattern` trigger automatikusan újragenerálta a pattern-family + exact_pattern derived tageket; a manuális tagek érintetlenek. Élő DB-n alkalmazva + ellenőrizve (0 legacy minta maradt). Nincs TS-kódváltozás → build/lint/teszt érintetlen.

---

## 4. Jövőbeni fejlesztések (feature backlog)

> Ide írjuk az új ötleteket. Minden tételhez: rövid leírás + prioritás.

- [ ] **denomailer leváltása saját MIME + SMTP rétegre** 🟢 (alacsony prioritás, 2026-08-01): a `send-fms-report` jelenleg működik, de a denomailer 1.6.0 fejléc-kezelése alapvetően hibás — a `quotedPrintableEncode` egy **törzs**-kódoló, amit fejlécre használ (74 karakterenként `=\r\n`, vezető whitespace nélkül), a `startsWith("=?")` ág pedig a kész encoded-wordöket is újracsomagolja. Ezt jelenleg egy vezető szóközös kerülőúttal védjük ki (`mailer.ts` / `encodeHeaderValue`). Két látható ára van: a `To:` fejlécben nem küldünk megjelenített nevet, és a `FMS_REPORT_FROM_NAME` csak ASCII lehet. Saját MIME-építéssel + közvetlen SMTP-párbeszéddel (Deno TLS socket, AUTH LOGIN — kb. 120 sor) mindkettő megszűnne, és egy hibás, nem karbantartott függőség is kikerülne. A 1.6.0 a legfrissebb kiadás, tehát felfelé nincs hova frissíteni.
- [ ] _(ide jönnek az új funkció-ötletek)_

---

## 5. Done (lezárt tételek)

- [x] **Gyakorlattár: FMS gyakorlatnevek magyarra fordítva** (2026-09-19): az admin-átnézés (lásd fentebb az „ellenőrizve" checkboxot) közben kiderült, hogy a teljes `fms` kategória (40/40 sor) angol `name` mezővel szerepelt (pl. „Ankle dorsiflexion mobilization", „Bird dog", „Thread the needle"), miközben a `description`/`instructions` mezők már magyarok voltak — ez feltehetően egy angol nyelvű FMS-korrekciós forrásból történt seedelés maradványa. Az `smr` kategória (14 sor) ellenőrzésre került és már teljesen magyar volt, ott nem kellett módosítás. Élő Supabase adatmódosítás (`UPDATE ... WHERE category = 'fms'`, kizárólag a `name` oszlopon, egyetlen tranzakcióban) — nem migráció, mert a gyakorlat-seed adatok eddig sem éltek migrációs fájlban (a `grep` a `supabase/migrations/`-ban nem talált rájuk hivatkozást), így egy friss DB-felállásnál sincs mit visszaállítani. Ütközés-ellenőrzés: 40 egyedi új név, duplikáció nincs. Névadási elvek: a Gray Cook / Pallof-féle gyakorlatneveknél a tulajdonnév megmaradt (pl. „Cook-féle csípőemelés", „Pallof press szalaggal"), a meglévő app-konvenciókhoz igazodva (pl. „Négykézláb…", „…szalaggal", „…tartás"). Kódváltozás nem történt, csak adattartalom — build/lint/teszt érintetlen.
- [x] **Gyakorlattár: „ellenőrizve" checkbox admin-átnézéshez** (2026-09-19): a gyakorlattár teljes átnézéséhez admin most soronként bejelölheti, hogy egy gyakorlatot már ellenőrzött. Új `reviewed BOOLEAN NOT NULL DEFAULT false` oszlop az `exercises` táblán (`20260919120000_add_exercise_reviewed_flag.sql` — meglévő gyakorlatok alapból ellenőrizetlenként indulnak); a `types/supabase.ts` és a `Database['exercises']` sor/insert/update típusai frissítve. `exerciseService.ts`-ben új `setExerciseReviewed(id, reviewed)` (önálló Supabase update, nem a teljes `updateExercise`, hogy a checkbox pörgetés ne fusson neki a taxonómia-újramentésnek). `ExerciseCard.tsx`: admin nézetben checkbox („Ellenőrizve") a kártya fejlécében, optimista frissítéssel az `ExerciseLibrary.tsx`-ben (hibánál toast + visszaállítás); nem-admin nézetben csak egy statikus badge jelzi az ellenőrzött állapotot. `ExerciseFilter.tsx` új „Ellenőrzöttség" választóval (Összes / Csak ellenőrizetlen / Csak ellenőrzött), a `filterExercisesList` (`exerciseTaxonomy/filters.ts`) `reviewedFilter` mezővel bővült — így az admin leszűrheti a hátralévő, még ellenőrizetlen gyakorlatokat. 2 új unit teszt a `taxonomyFilters.test.ts`-ben (164 teszt összesen); build, lint (0 error), `tsc --noEmit` zöld. A migrációt még nem alkalmaztuk az éles Supabase projekten — a fájl a `supabase/migrations/`-ban vár a szokásos deploy-lépésre.
- [x] **FMS korrekciós javaslat oldalkülönbségre is** 🔴 (2026-08-01): az előző tétel után a fejléc már helyesen jelzett korrekciót egy 3 / 2-es akadálylépésnél, de a „Javasolt korrekciós gyakorlatok" szekció **üres maradt** — a `getFMSCorrectionsForAssessment` (és az edzésgenerátort kiszolgáló `identifyFMSCorrections`) kizárólag a `score < 2` feltételt nézte, az aszimmetrikus 3 / 2 beszámított pontja viszont 2. Javítás a [fmsCorrections.ts](src/lib/workoutGenerator/fmsCorrections.ts)-ben: közös `getCorrectionReasons` helper, a csoport új `reasons` (`'low_score'` / `'asymmetry'`) és `sides` mezőkkel; korrekciót kap minden 2 pont alatti **vagy** oldalkülönbséget mutató minta. A megjelenítés megkülönbözteti a kettőt: a webes nézetben a pontszám csak gyenge eredménynél piros, mellette „oldalkülönbség: bal X / jobb Y" warning-chip; a PDF-ben a fejléc-badge kiírja az oldalankénti pontokat és warning-színt kap; a bevezető szöveg mindkét helyen említi az aszimmetriát. Az edzésgenerátor is kap korrekciós gyakorlatot ilyenkor (a `identifyFMSCorrections` ugyanezt a predikátumot használja, a véletlen gyakorlatválasztás megmaradt). 5 új unit teszt (`fmsCorrections.test.ts` — köztük a bejelentett 3 / 2 eset, a szimmetrikus 2 / 2 negatív esete és a két indok együttállása); 162 teszt, build, lint (0 error), `tsc --noEmit` zöld. A régi, oldalankénti adat nélküli felméréseknél a viselkedés változatlan (nincs mihez képest aszimmetriát mérni).
- [x] **FMS riport-értékelés javítása: nem csak az összpontszám számít** 🔴 (2026-08-01): a riport és a levél szöveges összegzése kizárólag a pontsávból (`FMS_SCORE_BANDS`) jött, ezért egy 18 / 21-es felmérés akkor is azt állította, hogy „a mozgásminták összességében stabilak", ha az aktív nyújtott lábemelés az egyik oldalon **1 pontos** volt és **aszimmetriát** mutatott — a fejléc tehát ellentmondott a saját táblázatának és a korrekciós szekciójának. Új tiszta réteg: [src/lib/fmsReport/risk.ts](src/lib/fmsReport/risk.ts) (`FMS_RISK_LEVELS`, `resolveFMSRisk`) — három szint (`ready` / `corrective` / `restricted`), ahol a végső szint a legrosszabb indoké: a pontsávból jövő alapszintet a **0 pont / pozitív clearing teszt** `restricted`-re, az **1 pontos minta** vagy bármely **oldalkülönbség** legalább `corrective`-re rontja. A modell `risk` mezőt kapott (`buildReportModel.ts`), a `band` megmarad, de már csak numerikus kontextus. A megjelenítés mindhárom helyen az értékelést mondja: `FMSReportView` fejléc (badge + összegzés + tételes „miért" felsorolás), a PDF kártyája + új „Mi indokolja ezt az értékelést?" doboz (`pdf.ts`, a kártya magassága a szöveghez igazodik), és az e-mail törzse (`fmsReportEmail.ts`: „Értékelés: …" + „Kiemelt megállapítások" blokk). Az eredménylista kártyái ugyanezt a címkét kapták: a `listFMSAssessmentSubjects` mostantól a legutóbbi **teljes** felmérés-sort adja vissza (`latestAssessment`), különben a lista is a félrevezető pontsáv-címkét mutatta volna; a badge-osztályok közösek (`components/fms/riskStyles.ts`). 8 új unit teszt (`fmsReportRisk.test.ts`, köztük pontosan a bejelentett 18 / 21 + 1 pontos aszimmetrikus ASLR eset) + 2 új e-mail teszt; 157 teszt, build, lint (0 error) és `tsc --noEmit` zöld. Migráció nem kellett (a szabály levezetett, nem tárolt). Szándékosan kimaradt: a régi, oldalankénti adat nélküli felméréseknél az aszimmetria továbbra sem állapítható meg — ott az értékelés a 0/1 pontokra és a pontsávra épül.
- [x] **FMS riport: modern PDF + e-mail küldés saját Gmail-lel** (2026-08-01): az FMS felmérés eddig egyirányú utca volt — a wizard elmentette a pontszámokat, de nem volt semmilyen eredménynézet vagy export. Új admin oldalak: `/fms-results` (alanylista, `FMSResultsPage`) és `/fms-results/:userId` (riport dátumválasztóval, `FMSReportPage`) + sidebar-nav (`canAccessFMSResults`). Tiszta domain a `src/lib/fmsReport/`-ban: `constants.ts` (`FMS_TEST_ORDER`, `FMS_SCORE_BANDS` — a korábban csak dokumentációban létező 14-21/10-13/0-9 sávok kódba emelve, `FMS_TEST_DESCRIPTIONS`, PDF hex-paletta a világos téma CSS-változóiból), `buildReportModel.ts`, `pdf.ts`, `fonts.ts`. **Magyar ékezet-buktató**: a jsPDF beépített fontjai cp1252-esek, amiben nincs `ő` (U+0151) és `ű` (U+0171) — a Google Fonts viszont már csak _variable_ Manrope-ot szállít (alapsúlya ExtraLight), ezért fonttools-szal statikus 400/700 instance-eket generáltunk latin-extended-A-ra szűkítve (48 KB/súly a teljes 110 helyett). A kész PDF ToUnicode CMap-je ellenőrizve: minden magyar karakter helyesen leképezve, a szöveg kereshető és másolható. A riport 2 oldal, ~25 KB. E-mail: új `supabase/functions/send-fms-report/` (Gmail SMTP 465/implicit TLS denomailerrel, `EMAIL_TRANSPORT` secrettel cserélhető Resendre; kötelező admin-ellenőrzés, hogy a fiók ne legyen levélküldő relay). Kliens: `src/lib/fmsReportEmail.ts`, UI: `components/fms/{FMSScoreGrid,FMSReportView,SendFMSReportDialog}.tsx`. A címzett a profil e-mailjéből előtöltve, de küldés előtt átírható (a `manual_guests`-nek nincs e-mail oszlopa). Determinisztikus korrekciókhoz új `getFMSCorrectionsForAssessment` — az `identifyFMSCorrections` szándékosan véletlenszerű, riportba alkalmatlan lett volna. Beállítási útmutató: [docs/fms_report_email_setup.md](docs/fms_report_email_setup.md). Diagram szándékosan kimaradt (egyeztetés szerint). Build + lint (0 error) + typecheck + teszt zöld. **Utólagos doc-javítás (2026-08-01)**: a B1/kulcscsere szekció `<placeholder>` írásmódja félrevezető volt — a `<` és `>` a shellben átirányítás-operátor, így a bemásolt `npx supabase secrets set` némán rossz dolgot csinált (nem állította be a secreteket). Mindkét parancsminta átírva kacsacsőr nélkülire, a jelszó `read -rs`-sel bekérve (zsh és bash forma is dokumentálva), így a shell history-ba sem kerül bele. **Éles élesítés + `close()` bugfix (2026-08-01)**: a függvény deployolva (v4, ACTIVE), a 4 secret beállítva. Az első éles küldés 502-vel bukott — a napló szerint `TypeError: Cannot read properties of undefined (reading 'catch')` a `mailer.ts` `finally` ágában: a denomailer 1.6.0 `client.close()`-a **`undefined`-ot ad vissza, nem Promise-t**, így a `.catch(() => {})` elszállt, és mivel `finally`-ben volt, a **már sikeres** küldést is hibává írta felül (a levelek valójában kimentek). Javítva `try/catch` + `await client.close()`-ra, ami mindhárom esetet lefedi (undefined / szinkron dobás / reject). A doc hibaelhárító szekciója is javítva: a `supabase functions logs` alparancs a telepített CLI v2.24.3-ban **nem létezik**, és a request-log nem tartalmazza a `console.error`-t — helyette a Dashboard Logs fül, illetve a Management API `function_logs` lekérdezése van dokumentálva. **RFC 2047 fejléc-kódolás javítása (2026-08-01)**: a megérkezett levélben csatolmány helyett a nyers MIME-forrás jelent meg. Ok: a denomailer Q-kódolása a **szóközt nem escape-eli**, így `=?utf-8?Q?Kácsor Zsolt?=` állt elő — a szóköz lezárja az encoded-wordöt, a sortörés a kódolt egység közepére esett, és a romlott `Subject` fejléc **idő előtt lezárta a fejléc-blokkot**, amitől a `Content-Type: multipart/mixed` is törzsszöveggé vált (a PDF sosem kapcsolódott be csatolmányként). Javítás: saját base64 („B") encoded-word kódoló a `mailer.ts`-ben (`encodeHeaderValue`) — a base64-ben szóköz nem fordulhat elő, az egységek kódpont-határon törnek (nem hasad ketté többbájtos karakter) és 75 karakter alatt maradnak. Mellékhatásként a fejléc-injektálás is kizárva: a `recipientName`-ben lévő `CR`/`LF` a nem-ASCII mintára illeszkedik, tehát base64-be kerül. A base64 önmagában **nem volt elég** (v5): a denomailer `quotedPrintableEncodeInline`-ja a `data.startsWith("=?")` ág miatt a kész encoded-wordöt is újracsomagolta (`=` → `=3d`), a mögötte lévő `quotedPrintableEncode` pedig egy **törzs**-kódoló, ami 74 karakterenként `=\r\n` soft line breaket szúr be — fejlécben ez vezető whitespace nélküli sortörés, és **ez zárta le idő előtt a fejléc-blokkot**. Végleges megoldás (v6): a fejléc-értéket úgy állítjuk elő, hogy a lib `return data` ágára fusson — saját base64 encoded-word **egyetlen vezető szóközzel** (így se nem-ASCII, se `=?` kezdet), a címzettnél pedig elhagyjuk a megjelenített nevet (ott `name.trim()` levágná a szóközt). A viselkedés a denomailer forrásából átemelt szűrővel, node-dal validálva: tárgy, teljes magyar ábécé és `\r\nBcc:` injektálási kísérlet — mindhárom érintetlenül átmegy, körbeér, és az egységek 75 karakter alatt maradnak. **Végponttól végpontig zöld** (2026-08-01): a riport megérkezik, a PDF kattintható csatolmányként jelenik meg, az ékezetek jók a tárgyban és a törzsben is. Ismert, tudatos kompromisszum: a `To:` fejlécben nincs megjelenített név (a `parseSingleEmail` `name.trim()`-je levágná a védő szóközt), és a `FMS_REPORT_FROM_NAME`-nek ASCII-nak kell lennie — mindkettő dokumentálva. Ha ezek zavaróvá válnának, a következő lépés a denomailer leváltása saját MIME-építésre + SMTP-párbeszédre (~120 sor), lásd a 4. szekciót.
- [x] **FMS pontozás javítása: oldalankénti mérés + clearing tesztek** (2026-08-01): a felmérés eddig mind a 7 tesztet szimmetrikusként kezelte, holott az FMS protokollban **5 teszt oldalanként pontozódik** és a beszámított pont a **gyengébb oldal**, továbbá **3 clearing (fájdalom-provokációs) teszt** pozitív eredménye kötelezően 0-ra viszi az érintett teszt pontját. Tiszta szabályok: [src/lib/fmsScoring.ts](src/lib/fmsScoring.ts) (`FMS_SIDED_TEST_IDS`, `resolveSidedScore`, `FMS_CLEARING_TESTS`), unit-tesztelve. Migráció `20260801130000`: új `<teszt>_left`/`_right` oszlopok a nyers oldalankénti pontoknak és `sm_clearing`/`tspu_clearing`/`rs_clearing` a clearing tesztekhez — a 7 meglévő oszlop marad a **beszámított** pont (a generált `total_score` erre épül). Régi felméréseknél az új oszlopok `null`-ok, és szándékosan nem találjuk ki őket visszamenőleg. Űrlagréteg: [src/lib/fmsAssessmentForm.ts](src/lib/fmsAssessmentForm.ts), UI: `components/fms/{FMSScoreSelector,FMSMovementStepCard}.tsx`. A pontszámok mostantól **üresen indulnak, nem 0-val** — a 0 az FMS-ben „fájdalom", azt véletlenül nem szabad menteni.
- [x] **FMS korrekciós gyakorlat-adatbázis** (2026-08-01): a korábbi korrekciós javaslatok általános szövegek voltak („Csípő mozgékonyság javítása"). Helyettük `src/lib/workoutGenerator/fmsCorrectionExercises.ts`: mind a 7 mozgásmintához **pontosan egy konkrét gyakorlat modalitásonként**, fix sorrendben SMR → FMS szalag → saját testsúly → kettlebell (oldás → mobilizálás → aktív stabilizálás → terhelt megerősítés), mindegyikhez `name` / `dosage` / `cue`. Ezek jelennek meg a riport „Javasolt korrekciós gyakorlatok" szekciójában (PDF + web) és az edzésgenerátorban. A négy modalitás szándékos megkötés — a `FMSCorrectionModality` uniót ne bővítsük; a szerkezetet unit teszt őrzi.
- [x] **FMS vendég ↔ adatbázisos alany összekötés** (2026-08-01): a `profiles.full_name` a legtöbb soron `null` (a nevek a `manual_guests`-ben élnek), ezért a riportokon és a választókon e-mail cím vagy csonka azonosító jelent meg név helyett. Új [src/lib/fmsSubjectLinking.ts](src/lib/fmsSubjectLinking.ts) (unit-tesztelt): `resolveSubjectName`, `formatSubjectOptionLabel`, `findGuestForSubject`, `findSubjectIdForGuest`. Az `FMSAssessment` űrlapon a két select **kétirányúvá** vált: bármelyik kitölthető elsőként, a másik automatikusan beáll (kapcsolat, illetve ékezetfüggetlen névegyezés alapján). Névegyezésnél csak **még nem kapcsolt** vendéget veszünk át, hogy azonos nevű, más alanyhoz kötött vendéget ne írjunk felül.
- [x] **FMS RLS javítás: az admin-policy sosem illeszkedett** (2026-08-01): az `fms_admin_all` policy `auth.jwt() ->> 'user_role' = 'admin'`-t vizsgált, de ez a claim egyetlen usernél sincs beállítva és custom access token hook sincs — a policy tehát **halott volt**. Következmény: az admin nem tudta más felhasználó FMS felmérését sem olvasni, sem menteni (a `/assessment` oldal `user_id: linkedUserId`-vel szúr be, amit az `fms_insert_own` blokkolt). Javítás: `20260801100000` migráció a `public.current_user_is_admin()` `STABLE SECURITY DEFINER` helperrel (rögzített `search_path`). Élő DB-n validálva rollback-elt tranzakciókban: admin mind a 4 sort látja (köztük 2 másik felhasználóét) és be tud szúrni más nevében; sima user továbbra is csak a saját 2 sorát, nulla szivárgással. A helper szándékosan **nem** `is_admin` néven fut: létezik már egy `is_admin(uuid)` → `get_user_role(uuid)` páros, ami `VOLATILE` plpgsql rögzített `search_path` nélkül, tehát RLS policy-be alkalmatlan.
- [x] **`profiles` RLS végtelen rekurzió javítása** 🔴 (2026-08-01): a `20260731120000` migráció admin-policy-ja inline `SELECT ... FROM public.profiles` subqueryt tartalmazott a `profiles` **saját** policy-jában, ami `42P17 infinite recursion detected in policy`-t okozott a tábla **minden** lekérdezésénél — a saját profil olvasásánál is. Néma tünet: a kliens nem kapta meg a `role`-t, ezért **az összes admin menüpont eltűnt a sidebarból**. Javítás: `20260801120000` migráció, a rekurzió a `public.current_user_is_admin()` `SECURITY DEFINER` helperrel strukturálisan lehetetlenné téve (a függvény a tulajdonos jogán fut, rá a `profiles` RLS-e nem vonatkozik). **Szabály lett belőle** a CLAUDE.md-ben: admin-policy-ba soha ne kerüljön inline `profiles` subquery.
- [x] **`workout_logs` integráció: mobil edzés-teljesítés megjelenítése** (2026-06-28): a web app most a `workout_logs` táblát is lekérdezi és megjeleníti a teljesítési státuszt. Új típusok és függvény: `WorkoutLog`, `WorkoutWithLog` (extends Workout), `computeLogDuration`, `getWorkoutsWithLogs` (`src/lib/workouts.ts`). Érintett oldalak: Edzésnapló (`WorkoutLog.tsx`) — zöld „Teljesítve + dátum + időtartam" badge a kártyákon; Edzéseim (`MyWorkouts.tsx`) — completion logika most a logokból jön (nem a sections.completed mezőkből), összesítők és szűrők frissítve; Program részletlap (`ProgramDetailPage.tsx`) — session soroknál zöld badge + eltelt idő; Edzésnaptár (`WorkoutCalendar.tsx`, `WorkoutCalendarPage.tsx`) — teljesített napokon zöld dot/háttér, részletpanelben badge. `programService.ts`: `getProgramWithWorkouts` most `WorkoutWithLog[]`-ot ad vissza. Migráció nem szükséges — a tábla és az RLS policy-k már megvannak. Build + lint (0 error) + 61 teszt zöld.
- [x] **Gyakorlattár taxonómia-revízió: KB-címke + mozgásminta-normalizálás** (2026-06-27): a teljes gyakorlattár (161 gyakorlat) átnézve. **(A)** Mozgásminta-drift: 24 erőedzés-gyakorlat legacy `movement_pattern`-je a kanonikus enum-értékekre képezve (`20260627123000` migráció) — visszaálltak a pattern-family/exact-pattern derived tagek (pl. Plank → `anti_extension`+`core`, Windmill/TGU → `anti_flexion`+`core`, Farmer járás → `gait`). **(B)** Kettlebell-szűrhetőség: 14 Erőedzés-kategóriás KB-gyakorlat manuális `kettlebell` taget kapott (`20260627124000` migráció) — 5 egyértelmű (Goblet guggolás, Windmill, TGU, Batwing, Forró krumpli) + 9 KB-vel tipikusan végzett (egylábas felhúzás/evezés, egykezes evezés, farmer járás ×2, push press, libikóka padlón nyomás, jó reggelt, áthúzás), edzői jóváhagyással. Erőedzés-kategóriás kettlebell-címkés gyakorlatok: 17 → 31. **(C)** Élő-adat anomália javítva: a kettlebell-kategóriás „Bicepsz emelés KB" sosem szinkronizált derived kettlebell tagje pótolva (teljes derived re-sync; idempotens, a create-migráció amúgy is tartalmazza). Nincs TS-kódváltozás → build/lint/teszt érintetlen.
- [x] **Gyakorlattár kategória-szűrő javítása** (2026-06-27): a szűrő nem találta a legnagyobb csoportot, mert az `exercises.category` oszlopban legacy seed-értékek maradtak, amelyek nem egyeztek a UI kanonikus kategória-listájával: `strength` (79 sor, a UI csak `strength_training`-et kínál) és `core` (20 sor, nincs ilyen UI-kategória) → 99 gyakorlat kiesett a szűrésből. Mivel a derived-taxonómia trigger is csak `strength_training`-et ismer, ezeknél a taxonómia-fallback is elbukott. Javítás: új `20260627122000_normalize_legacy_exercise_categories.sql` migráció a `strength`/`core` → `strength_training` normalizálással (a `core` döntés alapján Erőedzésbe olvad; a core-jelleg a mozgásminta-szűrőben marad). Az `AFTER UPDATE OF category` trigger automatikusan újragenerálta a 99 `strength` derived kategória-taget. Élő DB-n alkalmazva + ellenőrizve (kategória-eloszlás kanonikus, 99 derived strength tag). Nincs TS-kódváltozás → build/lint/teszt érintetlen. Kapcsolódó nyitott találat (mozgásminta-drift) a 3. szekcióban rögzítve.
- [x] **Edzéstervező UX-finomítás + microciklus-generálás** (2026-06-25): **(A)** Input-UX inkrementális csiszolás: a `WorkoutSectionsEditor` gyakorlat-kártyái összecsukhatók (kitöltött gyakorlat alapból összegző-fejléccé csukva), a speciális szűrők rejtett `<details>`-e felfedezhetőbb (chevron + „N aktív" badge), a `WorkoutPlanner`-en vékony **sticky összegző sáv**, generált tervnél a szekciók 4 fölött alapból csukva + „Mind kinyit/becsuk" gomb, a Résztvevők/FMS blokk **összecsukható accordionban**. **(B)** Microciklus (2/4/6 hetes program) egy gombbal a Periodizált/Pwron/Longevity panelekről: új `workout_programs` tábla + `workouts.program_id`/`program_week`/`program_day_label`/`program_sequence` FK-oszlopok (migráció + élő Supabase apply, security advisor tiszta a táblára), tiszta `buildMicrocyclePlan` logika (hetek × edzésnapok dátumozással + heti clamp), `microcycleGenerator` orchestrátor (a meglévő generátorokat hívja végig és batch-menti), `programService` CRUD, új `ProgramsPage`/`ProgramDetailPage` (`/programs`, `/programs/:id`) + sidebar-nav. 9 új unit teszt a tervező-logikára (61 teszt zöld). A Sablon generátor szándékosan kimaradt. Build + lint (0 error) + tsc + teszt zöld.
- [x] **Longevity generátor (4 hetes protokoll)** (2026-06-25): új `longevityWorkoutGenerator.ts` a „4 hetes Longevity edzésprotokoll" spec alapján — heti hármas sablon (Hétfő STRENGTH/HIGH CNS, Szerda STATO_DYNAMIC/LOW, Péntek AGT/LOW) hétről hétre progresszióval (erő: szett+terhelés, stato: idő+terhelés, AGT: körök +3/hét). Single-session kimenet (hét 1-4 + modalitás a UI-ban választva), mint a Pwronnál; AGT eszközváltozat (kettlebell swing alapértelmezett / Airdyne / dombfutás). Beépített invariáns-validáció (spec 9. fej.: CNS-sorrend, AGT 130 pulzusplafon, progresszió-korlát) figyelmeztetésként a notes-ban. Wiring: `PlannerMode` bővítve, `LongevityGeneratorPanel`, route `/workout-planner/longevity-generator` + page, nav (header + sidebar), WorkoutPlanner generálási ág. 11 új unit teszt a tiszta logikára (`buildLongevitySessionMeta`, `validateLongevitySession`) — 52 teszt zöld. A spec opcionális műszak-moduláció rétege (10. fej., `[S]`) szándékosan kimaradt. Build + lint + tsc zöld.
- [x] **Minőség/stabilitás csomag** (2026-06-25): Vitest 2 + 41 unit teszt (generátor + taxonómia tiszta logika), CI `npm test` lépés, 17 natív `alert()` → `react-hot-toast` egységesítés (4 komponens), `.env.example` ellenőrzés, teljes RLS audit (migrációk + élő Supabase advisor — tiszta a 21 saját táblán). Új találatok rögzítve a 3. szekcióban: függvény `search_path` hardening, séma-drift (`goals`/`goal_completions`/`pending_invites` migráció nélkül), két párhuzamos toast-rendszer. Build + lint + tsc + teszt zöld.
- [x] **`exerciseService.ts` modulokra bontása** (2026-06-25): a 869 soros service 225 sorra csökkent (-74%); a `.ts` továbbra is a belépési pont (megtartja a Supabase CRUD-ot — `getExercises`/`getExerciseById`/`createExercise`/`updateExercise`/`deleteExercise`/`listExerciseTaxonomyTags` + a `replaceManualTaxonomyAssignments` helpert — és re-exportál minden publikus típust/függvényt, így a 9 importáló fájl egyikét sem kellett módosítani). Kiszervezve a `src/lib/exerciseTaxonomy/` mappába: `types.ts` (összes `export type`), `constants.ts` (kategória/mozgásminta-opciók + FMS-fókusz nevek + taxonómia-slug map-ek + szűrhető slug-halmazok), `mapping.ts` (`mapExerciseWithTaxonomy` + derived-slug + tag-getterek), `metadata.ts` (label/option getterek), `filters.ts` (`filterExercisesList` + kategória/minta-szűrők + `getExerciseFMSFocuses`). Build + lint zöld.
- [x] **`ProgressTracking.tsx` komponensekre bontása** (2026-06-25): a 748 soros oldal 141 sorra csökkent (-81%). A főkomponens megtartja az adatbetöltést (`loadWeights` + `useAutoRefresh`) és a kompozíciót. Kiszervezve: `lib/progressTrackingHelpers.ts` (`weightSchema` zod + `WeightFormData` + `CHART_METRICS` adatvezérelt metrika-konfiguráció + `buildChartData`/`buildChartOptions`/`getChartLabels` — az 5× ismételt `switch (activeChart)` egy config-tömbbé olvasztva), `components/progress/MeasurementForm.tsx` (saját `useForm`), `ProgressChartCard.tsx` (Chart.js + metrika-gombok), `ProgressStatsCard.tsx`, `RecentEntriesCard.tsx`. Build + lint zöld.
- [x] **`workoutGenerator.fixed.ts` modulokra bontása** (2026-06-25): az 1620 soros generátor 242 sorra csökkent (-85%); a `.fixed.ts` továbbra is a belépési pont (re-exportál minden publikus típust/függvényt, így egyetlen importáló fájlt sem kellett módosítani). Kiszervezve a `src/lib/workoutGenerator/` mappába: `types.ts` (típusok + `TRAINING_FOCUS_OPTIONS` + `getTrainingFocusLabel`), `exerciseCategorizer.ts` (konstansok + `categorizeExercises` + `getRandomExercise`/`getFirstAvailableExercise`), `fmsCorrections.ts` (`FMS_CORRECTIONS` + `identifyFMSCorrections`), `focusPresets.ts` (periodizációs presetek + `getFocusPreset` + `applyFocusPresetToSections`), `dayPlans.ts` (`generate2DayPlan`/`generate3DayPlan`/`generateDay1-4Plan`). A két publikus függvény közti duplikáció (alapsúly-kiegészítés, FMS-lekérés) közös helperekbe (`applyDefaultWeights`, `fetchFMSCorrections`) emelve. Build + lint zöld.
- [x] **`WorkoutPlanner.tsx` komponensekre bontása** (2026-06-25): a 2303 soros oldal 920 sorra csökkent (-60%). Kiszervezve: `lib/workoutPlannerHelpers.ts` (típusok + `workoutSchema` + `getPlaceholderExerciseMeta` + default factory-k), `hooks/useSectionExerciseFilters.ts` (per-exercise szűrőállapot + műveletek), `components/workouts/ParticipantSelector.tsx`, `WorkoutSummaryCards.tsx`, `WorkoutSectionsEditor.tsx` (kardió/komplex/standard szekció renderelés). A `Section`/`SectionExercise` típus a helpers-be került (a builderek importja átállítva, körkörös import megszüntetve). Lint + build zöld.
- [x] **Supabase teszt-/segéd-fájlok konszolidálása** (2026-06-24): 3 halott fájl törölve, 2 használt maradt; build zöld
- [x] **`userService.ts` → `users.ts` merge** (2026-06-24): egyetlen használt export átemelve, import átállítva, `userService.ts` törölve; build zöld
- [x] **`workouts.ts:473` gyakorlatnév-feloldás** (2026-06-24): batch `id -> name` lekérdezés a `getWorkoutProgressTrend`-ben; nincs több TODO/FIXME a kódban
- [x] **Markdown-takarítás** (2026-06-24): 29 db egyszeri „fix/summary/status/complete" jegyzet a `docs/legacy/`-ba archiválva (`git mv`, history megőrizve); a gyökérben a 42 md-ből 13 érdemi maradt (CLAUDE/BACKLOG/README + database.md + setup/deployment guide-ok). `database.md` (README hivatkozás) és `DEPLOYMENT_GUIDE.md` (`deploy.sh` hivatkozás) szándékosan maradt
- [x] **Kódbázis takarítás** (2026-06-24): `workoutGenerator.ts` + 6 stale fájl + 2 használaton kívüli dup-service törölve; 100 SQL/shell fix-szkript a `scripts/legacy/`-ba archiválva; CLAUDE.md/BACKLOG.md frissítve; build zöld
- [x] Polar integráció bevezetése (commit `05ce6a4`)
- [x] Cardió és kettlebell komplex hozzáadása (commit `55a7851`)
- [x] Edzésnaptár funkció (commit `d371990`)
- [x] Edzésmegosztó rendszer (commit `a76c42c`)
